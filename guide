; # 1. "지금부터 이 폴더에서 Git을 사용하겠다!"고 선언합니다.
; git init

; # 2. "현재 폴더의 모든 파일(.)을 저장할 준비를 해라"는 뜻입니다.
; git add .

; # 3. "준비된 파일들을 '첫 번째 버전'으로 사진 찍듯 찰칵 저장해라"는 뜻입니다.
; git commit -m "첫 FastAPI 테스트 프로젝트 설정"

; # 4. 이 프로젝트의 메인 나뭇가지(브랜치) 이름을 'main'으로 정합니다.
; git branch -M main

; # 5. 내 컴퓨터(로컬)와 아까 만든 GitHub 바구니 주소를 연결하는 선을 긋습니다.
; # ⚠️ 주의: 아래 주소 부분에 Step 2에서 복사한 본인의 GitHub 주소를 넣으세요!
; git remote add origin https://github.com/본인아이디/fastapi-demo.git

; # 6. "내 컴퓨터에 저장된 코드를 GitHub로 밀어 올려라(Push)!"는 뜻입니다.
; git push -u origin main